# 🛒 Tech Store - متجر التقنية الحديث

<div align="center">

![PHP](https://img.shields.io/badge/PHP-7.4+-777BB4?style=for-the-badge&logo=php&logoColor=white)
![MySQL](https://img.shields.io/badge/MySQL-5.7+-4479A1?style=for-the-badge&logo=mysql&logoColor=white)
![JavaScript](https://img.shields.io/badge/JavaScript-ES6+-F7DF1E?style=for-the-badge&logo=javascript&logoColor=black)
![TailwindCSS](https://img.shields.io/badge/Tailwind-3.0+-38B2AC?style=for-the-badge&logo=tailwind-css&logoColor=white)
![Version](https://img.shields.io/badge/Version-2.0-blue?style=for-the-badge)

**متجر إلكتروني احترافي لبيع المنتجات التقنية**

[المميزات](#-المميزات) • [التثبيت](#-التثبيت) • [الاستخدام](#-الاستخدام) • [API](#-واجهات-برمجة-التطبيقات) • [المساهمة](#-المساهمة)

</div>

---

## 📋 جدول المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [المميزات](#-المميزات)
- [التقنيات المستخدمة](#-التقنيات-المستخدمة)
- [هيكل المشروع](#-هيكل-المشروع)
- [المتطلبات](#-المتطلبات)
- [التثبيت](#-التثبيت)
- [الإعداد](#-الإعداد)
- [الاستخدام](#-الاستخدام)
- [واجهات برمجة التطبيقات](#-واجهات-برمجة-التطبيقات)
- [الأمان](#-الأمان)
- [Clean Code](#-مبادئ-clean-code)
- [المساهمة](#-المساهمة)
- [الترخيص](#-الترخيص)

---

## 🌟 نظرة عامة

**Tech Store** هو متجر إلكتروني حديث ومتكامل مصمم لبيع المنتجات التقنية. المشروع مبني بـ **PHP** النقي مع قاعدة بيانات **MySQL**، ويتميز بتصميم عصري باستخدام **TailwindCSS** وتأثيرات حركية متقدمة.

### ✨ لماذا Tech Store؟

- 🎨 **تصميم عصري** - واجهة مستخدم حديثة مع تأثيرات Glassmorphism
- 🔒 **أمان عالي** - حماية شاملة ضد XSS و SQL Injection
- 📱 **متجاوب تماماً** - يعمل بشكل مثالي على جميع الأجهزة
- ⚡ **أداء سريع** - كود محسّن وخفيف
- 🛠️ **سهل التخصيص** - كود نظيف ومنظم
- 🌐 **دعم كامل للعربية** - واجهة وتوثيق بالعربية

---

## 🎯 المميزات

### للمستخدمين 👥

- ✅ **تصفح المنتجات** - عرض جميع المنتجات مع إمكانية البحث والفلترة
- ✅ **تفاصيل المنتج** - صفحة تفصيلية لكل منتج مع صور ووصف كامل
- ✅ **سلة التسوق** - إضافة وإدارة المنتجات في السلة
- ✅ **إتمام الطلب** - نظام checkout متكامل
- ✅ **حساب المستخدم** - تسجيل دخول وإنشاء حساب جديد
- ✅ **تتبع الطلبات** - متابعة حالة الطلبات
- ✅ **تصميم متجاوب** - تجربة مثالية على الجوال والتابلت

### للمسؤولين �‍💼

- ✅ **لوحة تحكم شاملة** - Dashboard احترافي مع إحصائيات
- ✅ **إدارة المنتجات** - إضافة، تعديل، وحذف المنتجات
- ✅ **نظام طلبات احترافي** - عرض جميع الطلبات مع تفاصيل كاملة
- ✅ **إدارة الطلبات** - عرض ومتابعة جميع الطلبات مع إمكانية عرض التفاصيل
- ✅ **إحصائيات مفصلة** - تقارير المبيعات والأداء
- ✅ **إدارة المستخدمين** - التحكم في حسابات المستخدمين

---

## 🚀 الميزات الجديدة (v2.0)

- 🆕 **نظام طلبات محسن** - صفحة منفصلة لإدارة جميع الطلبات مع تفاصيل كاملة
- 🆕 **واجهة إدارية محسنة** - تصميم أكثر احترافية وسهولة في الاستخدام
- 🆕 **إعادة توجيه ذكية** - توجيه تلقائي للمستخدمين غير المسجلين إلى صفحة تسجيل الدخول
- 🆕 **أمان محسن** - حماية إضافية ضد الوصول غير المصرح به

---

## 🛠️ التقنيات المستخدمة

### Backend
- **PHP 7.4+** - لغة البرمجة الأساسية
- **MySQL 5.7+** - قاعدة البيانات
- **PDO** - للتعامل الآمن مع قاعدة البيانات

### Frontend
- **HTML5** - هيكل الصفحات
- **TailwindCSS 3.0** - إطار عمل CSS
- **JavaScript (ES6+)** - البرمجة التفاعلية
- **jQuery** - للتعامل مع AJAX
- **AOS** - تأثيرات الحركة
- **SweetAlert2** - رسائل تنبيه جميلة
- **tsParticles** - تأثيرات الخلفية التفاعلية

### الأدوات
- **Git** - نظام التحكم بالإصدارات
- **Composer** - إدارة الحزم (اختياري)

---

## 🗂️ هيكل المشروع

```
tech-store/
│
├── 📂 frontend/               # الواجهة الأمامية
│   ├── 📂 assets/            # الملفات الثابتة
│   │   ├── 📂 css/           # ملفات الأنماط
│   │   ├── 📂 js/            # ملفات JavaScript
│   │   │   ├── main.js       # الوظائف الأساسية
│   │   │   └── auth.js       # وظائف المصادقة
│   │   └── 📂 images/        # الصور
│   ├── 📂 includes/          # الملفات المشتركة
│   │   ├── header.php        # رأس الصفحة (Navigation)
│   │   └── footer.php        # تذييل الصفحة
│   ├── 📄 index.php          # الصفحة الرئيسية
│   ├── 📄 products.php       # صفحة المنتجات
│   ├── 📄 product-details.php # تفاصيل المنتج
│   ├── 📄 cart.php           # سلة التسوق
│   ├── 📄 checkout.php       # إتمام الطلب
│   └── 📄 login.php          # صفحة تسجيل الدخول
│
├── 📂 backend/                # الخلفية
│   ├── 📂 config/            # ملفات الإعدادات
│   │   ├── database.php      # إعدادات قاعدة البيانات
│   │   └── helpers.php       # الدوال المساعدة (20+ دالة)
│   ├── 📂 api/               # واجهات برمجة التطبيقات
│   │   ├── 📂 auth/          # API المصادقة
│   │   │   ├── login.php     # تسجيل الدخول
│   │   │   ├── register.php  # إنشاء حساب
│   │   │   └── logout.php    # تسجيل الخروج
│   │   ├── 📂 products/      # API المنتجات
│   │   │   ├── get_all.php   # جلب جميع المنتجات
│   │   │   └── get_by_id.php # جلب منتج محدد
│   │   ├── 📂 orders/        # API الطلبات
│   │   │   ├── create.php    # إنشاء طلب جديد
│   │   │   └── get_details.php # جلب تفاصيل الطلب
│   │   └── 📂 admin/         # API لوحة التحكم
│   │       ├── products_manage.php # إدارة المنتجات
│   │       ├── orders.php    # إدارة الطلبات
│   │       └── stats.php     # الإحصائيات
│   └── 📂 admin/             # لوحة تحكم المسؤول
│       ├── index.php         # الصفحة الرئيسية
│       ├── products.php      # إدارة المنتجات
│       └── orders.php        # إدارة الطلبات
│
├── 📂 database/               # قاعدة البيانات
│   ├── 📄 setup.sql          # ملف إنشاء قاعدة البيانات
│   └── 📄 add_order_items.sql # إضافة عناصر الطلبات
│
├── 📂 docs/                   # التوثيق
│   └── 📄 README.md          # هذا الملف
│
└── 📄 .gitignore             # ملفات Git المستبعدة
```

---

## � المتطلبات

### متطلبات النظام

- **PHP** >= 7.4
- **MySQL** >= 5.7 أو **MariaDB** >= 10.2
- **Apache** أو **Nginx**
- **PDO Extension** (مفعّل افتراضياً)

### متطلبات اختيارية

- **Composer** - لإدارة الحزم
- **Git** - للتحكم بالإصدارات
- **Node.js** - لأدوات التطوير (اختياري)

---

## 🚀 التثبيت

### 1. استنساخ المشروع

```bash
# باستخدام Git
git clone https://github.com/your-username/tech-store.git
cd tech-store

# أو تحميل ZIP وفك الضغط
```

### 2. إعداد قاعدة البيانات

```bash
# تسجيل الدخول إلى MySQL
mysql -u root -p

# إنشاء قاعدة البيانات
CREATE DATABASE tech_store CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# استيراد الجداول
USE tech_store;
SOURCE database/setup.sql;

# أو باستخدام phpMyAdmin
# - افتح phpMyAdmin
# - أنشئ قاعدة بيانات "tech_store"
# - استورد ملف database/setup.sql
```

### 3. تكوين الاتصال

افتح ملف `backend/config/database.php` وعدّل البيانات:

```php
define('DB_HOST', 'localhost');     // عنوان السيرفر
define('DB_NAME', 'tech_store');    // اسم قاعدة البيانات
define('DB_USER', 'root');          // اسم المستخدم
define('DB_PASS', '');              // كلمة المرور
```

### 4. تشغيل المشروع

#### باستخدام PHP Built-in Server (للتطوير)

```bash
php -S localhost:8000
```

ثم افتح المتصفح على: `http://localhost:8000`

#### باستخدام XAMPP/WAMP

1. انسخ المشروع إلى مجلد `htdocs` (XAMPP) أو `www` (WAMP)
2. شغّل Apache و MySQL
3. افتح: `http://localhost/tech-store`

---

## ⚙️ الإعداد

### إنشاء حساب مسؤول

قم بتشغيل هذا الاستعلام في قاعدة البيانات:

```sql
INSERT INTO users (username, email, password, role, full_name, phone) 
VALUES (
    'admin',
    'admin@techstore.com',
    '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', -- password: password
    'admin',
    'المسؤول',
    '0500000000'
);
```

**بيانات الدخول الافتراضية:**
- البريد: `admin@techstore.com`
- كلمة المرور: `password`

⚠️ **مهم:** غيّر كلمة المرور فوراً بعد أول تسجيل دخول!

### إضافة منتجات تجريبية

```sql
INSERT INTO products (name, slug, description, price, category_id, stock, image_url) VALUES
('لابتوب Dell XPS 15', 'dell-xps-15', 'لابتوب احترافي بمعالج Intel Core i7', 1299.99, 1, 10, 'assets/images/laptop1.jpg'),
('شاشة Samsung 27"', 'samsung-27', 'شاشة 4K بتقنية HDR', 399.99, 3, 15, 'assets/images/monitor1.jpg');
```

---

## 📖 الاستخدام

### للمستخدمين

1. **التصفح**: افتح الصفحة الرئيسية لعرض المنتجات المميزة
2. **البحث**: استخدم شريط البحث أو الفلاتر في صفحة المنتجات
3. **إضافة للسلة**: اضغط على "أضف للسلة" في أي منتج
4. **إتمام الطلب**: اذهب إلى السلة واضغط "إتمام الطلب"
5. **تسجيل الدخول**: سجل دخول أو أنشئ حساب جديد

### للمسؤولين

1. سجل دخول بحساب المسؤول
2. اذهب إلى `/admin`
3. استخدم لوحة التحكم لإدارة:
   - **الإحصائيات**: عرض المبيعات والعملاء
   - **المنتجات**: إضافة/تعديل/حذف المنتجات
   - **الطلبات**: عرض جميع الطلبات مع تفاصيل كاملة (انقر على "الطلبات" في الشريط الجانبي)
   - **عرض المتجر**: رابط سريع للعودة للمتجر

**حساب المسؤول الافتراضي:**
- البريد: admin@techstore.com
- كلمة المرور: password

---

## 🔌 واجهات برمجة التطبيقات

### المصادقة (Authentication)

#### تسجيل الدخول
```http
POST /api/auth/login.php
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

**الاستجابة:**
```json
{
  "success": true,
  "message": "تم تسجيل الدخول بنجاح",
  "data": {
    "user": {
      "id": 1,
      "username": "khalil",
      "email": "user@example.com",
      "role": "user"
    }
  }
}
```

#### إنشاء حساب
```http
POST /api/auth/register.php
Content-Type: application/json

{
  "username": "khalil",
  "email": "khalil@example.com",
  "password": "password123",
  "full_name": "خليل إبراهيم",
  "phone": "0500000000"
}
```

### المنتجات (Products)

#### جلب جميع المنتجات
```http
GET /api/products/get_all.php?page=1&limit=12&category=1&search=laptop
```

**المعاملات:**
- `page` (اختياري): رقم الصفحة (افتراضي: 1)
- `limit` (اختياري): عدد المنتجات (افتراضي: 12)
- `category` (اختياري): معرف الفئة
- `search` (اختياري): كلمة البحث

**الاستجابة:**
```json
{
  "success": true,
  "message": "تم جلب المنتجات بنجاح",
  "data": {
    "products": [...],
    "pagination": {
      "page": 1,
      "limit": 12,
      "total": 50,
      "pages": 5
    }
  }
}
```

#### جلب منتج محدد
```http
GET /api/products/get_by_id.php?id=1
```

### الطلبات (Orders)

#### إنشاء طلب جديد
```http
POST /api/orders/create.php
Content-Type: application/json

{
  "items": [
    {"product_id": 1, "quantity": 2},
    {"product_id": 3, "quantity": 1}
  ],
  "shipping_address": "الرياض، السعودية",
  "phone": "0500000000"
}
```

---

## 🔒 الأمان

المشروع يطبق أفضل ممارسات الأمان:

### ✅ الحماية المطبقة

1. **SQL Injection Protection**
   - استخدام Prepared Statements في جميع الاستعلامات
   - عدم استخدام string concatenation مع SQL

2. **XSS Protection**
   - تنظيف جميع المخرجات باستخدام `htmlspecialchars()`
   - دالة `recursiveEscape()` للبيانات المعقدة

3. **Password Security**
   - تشفير كلمات المرور باستخدام `password_hash()`
   - استخدام `password_verify()` للتحقق

4. **Session Security**
   - `session_regenerate_id()` بعد تسجيل الدخول
   - التحقق من الصلاحيات في كل طلب

5. **Input Validation**
   - دوال تحقق شاملة: `isValidEmail()`, `isValidLength()`, etc.
   - تنظيف المدخلات: `sanitize()`, `sanitizeInt()`, etc.

6. **Error Handling**
   - عدم عرض تفاصيل الأخطاء للمستخدمين
   - تسجيل الأخطاء في ملفات logs

### 🛡️ توصيات إضافية للإنتاج

```php
// في ملف .htaccess
# منع الوصول المباشر للملفات الحساسة
<FilesMatch "\.(log|sql|md)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# تفعيل HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

---

## 🎨 مبادئ Clean Code

المشروع يتبع مبادئ Clean Code بشكل صارم:

### 1. أسماء واضحة ومعبرة
```php
// ❌ سيء
function gp($id) { ... }

// ✅ جيد
function getProductById($id) { ... }
```

### 2. دوال صغيرة ومركزة
```php
// كل دالة تقوم بمهمة واحدة فقط
function sanitizeEmail($email) {
    $email = filter_var(trim($email), FILTER_SANITIZE_EMAIL);
    return filter_var($email, FILTER_VALIDATE_EMAIL) ? $email : false;
}
```

### 3. DRY (Don't Repeat Yourself)
```php
// بدلاً من تكرار الكود، استخدم الدوال المساعدة
$email = getParam('email');
$name = getParam('name');
```

### 4. تعليقات مفيدة
```php
/**
 * تنظيف البريد الإلكتروني والتحقق منه
 * 
 * @param string $email البريد الإلكتروني
 * @return string|false البريد المنظف أو false إذا كان غير صالح
 */
```

### 5. معالجة الأخطاء
```php
try {
    // الكود
} catch (PDOException $e) {
    logError("Database error: " . $e->getMessage());
    jsonResponse(false, 'حدث خطأ', null, 500);
}
```

📚 **للمزيد**: راجع ملف [CLEAN_CODE_GUIDE.md](CLEAN_CODE_GUIDE.md)

---

## 🤝 المساهمة

نرحب بمساهماتكم! إليك كيفية المساهمة:

### خطوات المساهمة

1. **Fork المشروع**
2. **أنشئ فرع جديد**
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. **قم بالتعديلات**
4. **Commit التغييرات**
   ```bash
   git commit -m 'إضافة ميزة رائعة'
   ```
5. **Push للفرع**
   ```bash
   git push origin feature/amazing-feature
   ```
6. **افتح Pull Request**

### معايير المساهمة

- ✅ اتبع مبادئ Clean Code
- ✅ أضف تعليقات واضحة بالعربية
- ✅ اختبر الكود قبل الإرسال
- ✅ حدّث التوثيق إذا لزم الأمر

---

## 📝 الترخيص

هذا المشروع مرخص تحت **MIT License** - راجع ملف [LICENSE](LICENSE) للتفاصيل.

```
MIT License

Copyright (c) 2024 Tech Store

يُسمح بالاستخدام والتعديل والتوزيع مجاناً
```

---

## 👨‍💻 المطور

**تم تطويره بواسطة:** فريق Tech Store

- 🌐 الموقع: [techstore.com](https://techstore.com)
- 📧 البريد: support@techstore.com
- 💼 LinkedIn: [Tech Store](https://linkedin.com/company/techstore)

---

## 🙏 شكر وتقدير

- **TailwindCSS** - إطار عمل CSS الرائع
- **SweetAlert2** - مكتبة التنبيهات الجميلة
- **AOS** - مكتبة تأثيرات الحركة
- **tsParticles** - تأثيرات الخلفية التفاعلية

---

## 📞 الدعم

إذا واجهت أي مشكلة أو لديك استفسار:

1. **افتح Issue** في GitHub
2. **راسلنا** على support@techstore.com
3. **راجع** ملف [CLEAN_CODE_GUIDE.md](CLEAN_CODE_GUIDE.md)

---

<div align="center">

**صُنع بـ ❤️ في السعودية**

⭐ إذا أعجبك المشروع، لا تنسَ إعطائه نجمة!

[⬆ العودة للأعلى](#-tech-store---متجر-التقنية-الحديث)

</div>
